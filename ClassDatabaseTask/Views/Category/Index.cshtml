@{
    ViewData["Title"] = "Index";
}

@if(@TempData["Msg"] != null)
{
    <p class="alert alert-success">@TempData["Msg"]</p>
}

<h1>Index</h1>
<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Details</th>
        <th>Update</th>
    </tr>

    @foreach(var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td><a href="/Category/Details/@item.Id">Details</a></td>
            <td><a href="/Category/Edit/@item.Id">Update</a></td>
        </tr>
    }
</table>
